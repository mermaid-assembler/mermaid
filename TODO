* go from 4-bit -> 2-bit
* Make it so that not all the files have to be present for each node
* Delete kmers from hash table as we gather
* lookinto mmap for performance reasons
* may want to add preproccessing step that uses 'split' to split files into
  small files that fit in memory using mmap
* For certain Illumina versions, stop reading after seeing a quality score of 2
